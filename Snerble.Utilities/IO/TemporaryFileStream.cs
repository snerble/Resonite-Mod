namespace Snerble.Utilities.IO;

public class TemporaryFileStream
{
	// TODO implement
	// should delete the file on eof or close


	// also, try to implement a partial file stream;
	//   basically a stream that can be read from while its still being written,
	//   and when the reader passes by the point of writing, it blocks.
	//   might even just work with straight files, but idk.
	// a stream like that can act as a great buffer for the music player
}
